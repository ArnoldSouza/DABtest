# This is a Databricks asset bundle definition for demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: rambase_daily_data

artifacts:
  default:
    type: whl
    build: poetry build
    path: .

include:
  - resources/*.yml

variables:
  cost_object:
    description: "Cost object for rambase development and implementation"
    default: "GPDI-DEV/EXPLORE-ALL_USERS"
  uc_catalog:
    description: "Unity Catalog target catalog for Rambase curated objects"
    default: "replace_with_uc_catalog"
  uc_schema:
    description: "Unity Catalog target schema for Rambase curated objects"
    default: "rambase"
  external_rambase_catalog_name:
    description: "Name for the catalog of the external rambase database (default test environment value)"
    default: "prod_gbs_gpdi_sqlserver_rambase"
  view_based_rambase_catalog_name:
    description: "Name for the catalog of the view-based external rambase database (default test environment value)"
    default: "replace_with_view_based_rambase_catalog"